### Run the project

With [Docker Compose](https://docs.docker.com/compose/) installed, run the following command:

```bash
docker-compose up
```

### Run tests

```bash
./gradlew dbTestsDown
./gradlew dbTestsWait
```