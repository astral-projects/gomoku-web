export type User = {
  id: Id;
  username: Username;
  email: Email;
};

export type Id = {
  value: number;
};

export type Username = {
  value: string;
};

export type Email = {
  value: string;
};
